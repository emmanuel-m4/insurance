name: New release
 # Deploy to environments, helm upgrade, kubectl apply, terraform apply
 
on:
  # push:
  #   branches: [main]
  release:
    types: [created]
    
# Elevated permissions 
permissions:
  contents: read
  packages: read
  # id-token: write

jobs:
  build-and-deploy:
    runs-on: self-hosted
    env:
      APP_NAME: "insurance"
      AZURE_WEBAPP_PACKAGE_PATH: "src/Server/publish"
      SERVER_PATH: "src/Server"

    steps:
      - uses: actions/checkout@v6
      - name: Placeholder to test release
        run: echo "This is a placeholder to test release"

      # - name: Azure Login (OIDC)
      #   uses: azure/login@v2
      #   with:
      #     client-id: ${{ vars.AZURE_CLIENT_ID }}
      #     tenant-id: ${{ vars.AZURE_TENANT_ID }}
      #     subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # - name: Publish with SBOM
      #   run: dotnet publish ${{env.SERVER_PATH}}/Insurance.Server.csproj -c Release --output ${{env.AZURE_WEBAPP_PACKAGE_PATH}} /p:GenerateSbom=true

    # - name: Upload SBOM to Blob Storage
    #   run: |
    #     az storage blob upload \
    #       --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
    #       --container-name sbom \
    #       --name manifest.spdx.json \
    #       --file "${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/sbom/manifest.spdx.json" \
    #       --auth-mode login

    # - name: Deploy to Azure Web App
    # uses: azure/webapps-deploy@v2
    # with:
    #  app-name: ${{env.APP_NAME}}
    #   publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
    #   package: ${{env.AZURE_WEBAPP_PACKAGE_PATH}}
